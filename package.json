{
  "homepage": "https://github.com/carlitosgithub/clicky-game",
  "name": "clicky-game",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.6.0",
    "react-dom": "^16.5.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^2.0.0"
  },
  "scripts": {
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
